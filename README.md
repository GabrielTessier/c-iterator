# Iterator

> [!WARNING]
> The library is not in production ready state

Custom iterator implementation in C.

To build the example:

```console
$ make
$ ./bin/test
```

## Supported platforms

- Linux x86_64
